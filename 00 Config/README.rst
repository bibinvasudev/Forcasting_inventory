Development Config (not in final production release)
----------------------------------------------------

This directory should contain the ``identifier.csv`` file during platform development
(see development setup).
